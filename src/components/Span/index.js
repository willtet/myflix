import styled from 'styled-components';

const Span = styled.span`
  color: red;
  background: var(--black);
  padding: 16px 24px;
  font-style: normal;
  font-weight: bold;
  font-size: 14px;
  outline: none;
  text-decoration: none;
`;

export default Span;